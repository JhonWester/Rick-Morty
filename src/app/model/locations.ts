import { StringifyOptions } from 'querystring';

export class Localization {
    id: number;
    name: string;
    type: string;
    dimension: string;
    residents: Array<string>;
    url: string;
    created: string;
}
